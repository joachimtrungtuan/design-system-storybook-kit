# Migration notes

Versioned migration notes shipped with `story-cli-kit` live in this directory.
Each implemented structural migration adds its corresponding `<version>.md` note here.

Migration behavior is implemented by the engine and CLI; this directory contains
the durable instructions bundled with the installed toolkit.
