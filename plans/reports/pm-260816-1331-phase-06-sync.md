## Project Status: 2026-08-17 00:20

| Plan | Progress | Priority | Status | Next action |
| --- | ---: | --- | --- | --- |
| story-cli-kit implementation | 57/137 (42%) | P1 | in-progress | Close remaining Phase 6 acceptance debt before Phase 7 |

### Phase 6 sync-back

- [x] Tracking only. Source implementation left untouched; pre-existing mode-only `dist/packages/cli/src/bin.js` change preserved.
- [x] Phase 6 implementation exists in `packages/cli/src/commands/{create,generate}.ts`, `packages/cli/src/create/*`, `packages/engine/src/manifest/*`, `packages/engine/src/scaffold/*`, and `materialise.ts`.
- [x] Source verification supplied: full source suite 120/120 pass; direct `tsc` typecheck pass; build pass; `git diff --check` pass.
- [x] Compiled `create --no-install` smoke supplied: manifest written, `appliedMigrations: []`, `tokens.css` generated, initial git commit made, deferred install/validate commands printed.
- [x] Focused `generate` evidence supplied: all four tiers validate.
- [x] Rollback defects repaired and evidenced: enclosing staged-path reset on commit failure; partial `git init` cleanup.
- [x] Local test inspection confirms tracked criteria for `src/` collision refusal, preserved README rollback safety, rendered-manifest marking, and `--no-install` reporting.
- [x] Real npm installation completed on a disposable project created by the compiled scaffold: 180 packages added, 182 audited, 0 vulnerabilities.
- [x] The installed local CLI now validates that project with `npm exec -- ds validate --json`; the package-root guard was fixed to support ESM-only `exports` resolution.
- [x] The installed project passes `npm run build` (Vite production build) and `npm run build-storybook` (Storybook production build).

### Pending acceptance

- [ ] The automatic `create`-owned install/tail-validation retry stalled in npm registry resolution before `node_modules` appeared; the same generated project was then installed manually and completed successfully.
- [ ] Vite dev-server and Storybook dev-server startup are still pending: the sandbox rejects localhost listeners with `EPERM`, and two narrow elevated approval attempts timed out before launch. Production builds and installed CLI validation are green.
- [ ] Full prompt matrix not closed: empty-dir no-extra-prompt proof, enclosing-repo default path, workspace messaging, and per-prompt cancel coverage remain incomplete.
- [ ] Full apply-stage failure matrix still incomplete; current evidence covers key rollback regressions, not every stage.
- [ ] Mandatory code-review capability hung after bounded retries; no independent verdict captured.
- [ ] Phase 6 criterion tying `generate` to later `ds update` barrel classification stays open until Phase 7 can verify the classifier side.

### Limits / blockers

- `pnpm` wrapper remains sandbox-blocked by the SQLite store permission. This sync relies on supplied direct `node`/installed `tsc` evidence instead of the normal wrapper path.
- Runtime acceptance is the main remaining risk. Without a full npm install + Vite/Storybook boot, Phase 6 cannot honestly move to complete.
- Starting Phase 7 early would stack classifier work on top of partially accepted scaffold behavior. Finish the Phase 6 plan first.

### Next actions

1. Maintainer/runtime-enabled environment: rerun the automatic `create` install path and local dev-server checks where localhost listeners are permitted.
2. Main agent: finish the prompt/cancel and apply-stage failure matrices. DoD: each remaining Phase 6 acceptance item is either checked with evidence or explicitly left pending with a reason.
3. Main agent: rerun the required review path with a bounded alternative if the same capability hangs again. DoD: independent review verdict or explicit blocker report.

### Unresolved questions

- None.
