# Agent skill

The installed toolkit's design-system agent skill lives in this directory.
Its `SKILL.md` and supporting references are introduced with the semantic-rule phase.

The skill consumes the mechanical validator first and then reviews the contract's
semantic rules; it does not replace or weaken validator results.
