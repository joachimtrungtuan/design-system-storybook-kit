import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Foundations/Colors" />

# Colors

Colour ramps are generated from anchors in `tokens.json`; semantic tokens point at those generated values.
